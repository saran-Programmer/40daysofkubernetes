apiVersion: v1
kind: Pod
metadata:
  name: nginx-toleration
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx
  tolerations:
  - key: "gpu"
    operator: "Equal"
    value: "true"
    effect: "NoSchedule"
